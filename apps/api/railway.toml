[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @offline/api build"
watchPatterns = []

[deploy]
startCommand = "pnpm --filter @offline/api start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
