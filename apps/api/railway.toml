[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @base-template/api build"
watchPatterns = []

[deploy]
startCommand = "pnpm --filter @base-template/api start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
